# Archivo de ejemplo de variables de entorno SOLO para el TRAINER
# (La API/main puede tener su propio .env con otras variables)

########################################
# Datos y artefactos (trainer)
########################################
# Ruta al CSV de entrenamiento
DATA_PATH=data/bank-full.csv
# Carpeta de salida para los artefactos (modelos)
OUTPUT_DIR=artifacts
# Separador del CSV (; , o vacío para autodetectar)
CSV_SEP=;
# Semilla
RANDOM_STATE=42
# Nombre de alias estable del modelo dentro de OUTPUT_DIR
ARTIFACT_ALIAS=model_latest.joblib

########################################
# MongoDB (registro de runs y GridFS)
########################################
# URI de conexión (por ejemplo: mongodb://localhost:27017)
MONGO_URI=
# Base de datos
MONGO_DB=
# Colección para guardar runs de entrenamiento
MONGO_TRAINING_COL=training_runs
# Subir el artefacto a GridFS automáticamente (true/false)
UPLOAD_GRIDFS=true

########################################
# Entrenamiento, calibración y tuning
########################################
# Objetivo de optimización para el umbral: f1 | fbeta | precision | recall | cost
OPTIMIZE=f1
# Beta para F-beta (solo si OPTIMIZE=fbeta)
BETA=1.0
# Calibración de probabilidades: sigmoid | isotonic | none
CALIBRATION=sigmoid
# Tuning del árbol (RandomizedSearchCV)
TUNE_SCORING=average_precision
TUNE_ITER=80
TUNE_FOLDS=5

########################################
# Rebalanceo de clases
########################################
# Estrategia: none | over | under | smote
RESAMPLE=none
# Ratio de muestreo (minoría/mayoría para over/SMOTE; fracción minoría objetivo para under)
RESAMPLE_RATIO=0.5
# Peso de la clase positiva (solo si no se rebalancea): deja vacío para omitir
POS_WEIGHT=

########################################
# Optimización por costo (opcional)
########################################
# Costos unitarios (usar si OPTIMIZE=cost)
COST_FP=
COST_FN=
