# Archivo de ejemplo de variables de entorno para la API (main)

########################################
# Carga del modelo en la API
########################################
# Si existe este archivo local, la API lo carga directamente.
# Si NO existe, la API intentará descargar el último modelo desde GridFS
# usando MONGO_URI/MONGO_DB y lo guardará en MODEL_CACHE_PATH.
PIPELINE_PATH=artifacts/decision_tree_model.joblib
MODEL_CACHE_PATH=artifacts/model_cached.joblib

########################################
# MongoDB (lectura de runs y descarga desde GridFS)
########################################
# URI de conexión (p.ej. mongodb://localhost:27017)
MONGO_URI=
# Base de datos
MONGO_DB=
# Colección con los runs de entrenamiento (debe existir en esa DB)
MONGO_TRAINING_COL=training_runs

########################################

########################################
# Lista de orígenes permitidos separada por comas.
# Usa * para permitir todos (solo desarrollo). En prod especifica tu(s) dominio(s).
ALLOW_ORIGINS=*
# Habilita credenciales solo cuando ALLOW_ORIGINS no es *
ALLOW_CREDENTIALS=false
